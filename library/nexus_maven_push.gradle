apply plugin: 'maven'

configurations {
  deployerJars
}

repositories {
  mavenCentral()
}

uploadArchives {
  repositories {
    mavenDeployer {
      repository(url: "http://192.168.1.18:8081/nexus/content/repositories/jxplayer/") {
        authentication(userName: "admin", password: "admin123")
      }
      pom.project {
        version '1.0.6'
        artifactId 'player'
        groupId 'com.jxkj.player'
        packaging 'aar'
        description 'library for android'
      }
    }
  }
}